# Basic environment for ACT.
name: act_env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - numpy
  - scipy
  - xarray
  - matplotlib
  - astral
  - dask
  - distributed
  - pandas
  - pint
